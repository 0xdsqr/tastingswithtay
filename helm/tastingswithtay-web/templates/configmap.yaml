apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tastingswithtay-web.fullname" . }}-config
  labels:
    {{- include "tastingswithtay-web.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
