apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tastingswithtay-admin.fullname" . }}-config
  labels:
    {{- include "tastingswithtay-admin.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
